# macOS
.DS_Store

# Config local do Claude Code (permissões da máquina)
.claude/settings.local.json

# Fontes de imagem — versionamos só os .avif gerados a partir delas
assets/*.jpg
assets/*.jpeg
assets/*.png
assets/*.webp
assets/*.gif
